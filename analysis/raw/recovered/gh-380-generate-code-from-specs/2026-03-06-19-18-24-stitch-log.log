Now let me read the existing `cmd/ts/main.go` and reference implementations:Now I have a clear picture. The file `cmd/ts/main.go` already exists with a substantial implementation. Let me verify it against the task requirements and acceptance criteria, then fix any issues.

Let me check for issues:
1. The `-m` flag is parsed but ignored (parameter named `_ bool`) — should document why
2. Need to verify compilation and behaviorGood, it compiles. Let me verify the acceptance criteria:Wait, the exit code for empty stdin shows blank — let me verify:The output is getting cut off. Let me try differently:The output seems to be getting eaten. Let me try a different approach:Exit code is 0. Now let me check for lint warnings:I need to fix the unchecked `fmt.Fprint` return values. Let me update the file:Now verify all acceptance criteria:All acceptance criteria pass:

- **AC1** ✅ — `go build ./cmd/ts/` compiles without errors
- **AC2** ✅ — Default format produces `Mar 06 19:21:10 hello` matching `%b %d %H:%M:%S` pattern
- **AC3** ✅ — `-s` flag produces elapsed time in `HH:MM:SS` format
- **AC4** ✅ — `-i` flag produces incremental time in `HH:MM:SS` format
- **AC5** ✅ — Custom format `%Y-%m-%d %H:%M:%S` produces `2026-03-06 19:21:12 hello`
- **AC6** ✅ — Exit code 0 on normal completion, SIGPIPE handled via `sys.InstallSIGPIPEHandler()`
- **AC7** ✅ — `golangci-lint run ./cmd/ts/` reports 0 issues

**Summary of changes:** Fixed 3 `errcheck` lint violations in `cmd/ts/main.go` by extracting a `writeLine` helper that checks the `fmt.Fprint` return value and exits cleanly on write errors. Also added the same error handling to `processRelative`. Added documentation comments explaining why the `-m` flag parameter is unused (Go's `time.Now()` monotonic reading handles it automatically per R5.1).