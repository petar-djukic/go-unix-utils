id: rel05.0-uc004-fold
title: "Wrap long lines to a fixed width via fold"

summary: |
  The operator runs the Go fold binary on one or more files or stdin. The binary wraps
  lines that exceed the maximum width by inserting newlines at the specified column or
  byte boundary, optionally breaking at word boundaries. The differential testing harness
  then runs both the Go binary and the Homebrew reference binary (gfold from coreutils)
  with identical inputs and confirms that their outputs are identical byte-for-byte.

actor: Human Operator

trigger: |
  The operator has completed the PRD (srd023-fold.yaml) and invokes do-work to produce
  a passing differential test for cmd/fold.

flow:
  - id: F1
    step: |
      The operator exercises fold in default width (80), custom width (-w), byte mode
      (-b), and space-break mode (-s). -b changes width measurement from column
      positions to raw bytes; -s changes the break strategy from hard column to
      last-space.
  - id: F2
    step: |
      The operator tests tab-containing input to verify correct column expansion for
      wrap-point calculation, and lines shorter than the width to confirm passthrough.
  - id: F3
    step: |
      The differential testing harness (pkg/testutils) executes both ./bin/fold and gfold
      with the same stdin, args, and environment. Requires F1 (binary must be built).

touchpoints:
  - T1: "cmd/fold — line width measurement, newline insertion, tab column expansion, space-break mode (srd023-fold R1, R2, R3)"
  - T2: "pkg/testutils — RunDiffTests, binary execution and byte-for-byte comparison"
  - T3: "pkg/sys — InstallSIGPIPEHandler for graceful SIGPIPE handling"

success_criteria:
  - id: S1
    criterion: |
      The differential test passes for default width, -w, -b, -s, -w -s combined,
      tab-containing input, and short-line passthrough, confirming full parity with gfold.
    traces:
      - AC1
      - AC2
      - AC3
      - AC4
      - AC5
      - AC6

out_of_scope:
  - Unicode multi-byte grapheme width calculation is not implemented; LC_ALL=C single-byte behavior is the requirement.
  - Lines without a trailing newline are handled by matching GNU behavior exactly.

test_suite: test-rel-05.0
