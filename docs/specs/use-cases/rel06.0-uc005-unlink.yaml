id: rel06.0-uc005-unlink
title: "Remove a single file via unlink"

summary: |
  The operator invokes the Go unlink binary to remove exactly one file by
  calling the unlink system call. Unlike rm, unlink accepts only a single
  path and refuses to operate on directories. The differential testing harness
  runs both the Go binary and the Homebrew reference binary (gunlink from
  coreutils) with the same arguments and compares stderr and exit codes.

actor: Human Operator

trigger: |
  The operator has completed the PRD (srd038-unlink.yaml) and invokes do-work to
  produce a passing differential test for cmd/unlink.

flow:
  - id: F1
    step: |
      The operator exercises unlink with a regular file, a symbolic link, a
      nonexistent path, a directory, zero arguments, and multiple arguments.
      unlink is deliberately minimal: it calls unlink(2) on exactly one path
      and rejects all other usage.
  - id: F2
    step: |
      The differential testing harness (pkg/testutils) executes both
      ./bin/unlink and gunlink with the same arguments and environment.
      Requires F1 (binary must be built). The harness compares stderr output
      and exit codes.

touchpoints:
  - T1: "cmd/unlink — single file removal, error on missing file, error on directory (srd038-unlink R1, R2)"
  - T2: "pkg/testutils — RunDiffTests, stdout comparison"

success_criteria:
  - id: S1
    criterion: |
      The differential test passes for regular file removal, symbolic link
      removal, nonexistent path errors, directory errors, zero-argument errors,
      and multi-argument errors, confirming full parity with gunlink.
    traces:
      - AC1
      - AC2
      - AC3
      - AC4
      - AC5
      - AC6

out_of_scope:
  - Unlinking symbolic links to directories; the test covers regular files and plain directories only.
  - Behavior when the file is open by another process (OS-dependent semantics).

test_suite: test-rel-06.0
