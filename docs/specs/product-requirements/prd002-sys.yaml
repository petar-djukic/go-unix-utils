id: prd002-sys
title: "pkg/sys: Syscall Abstractions"

problem: |
  Four utilities (ls, du, find, xargs) need OS-level capabilities that Go's standard
  library does not expose in a portable, stable form: terminal-width queries, extended
  file metadata (hard-link count, device and inode numbers, physical block counts),
  and signal handling for SIGPIPE and SIGWINCH. On Darwin and Linux these syscalls
  diverge in struct layout (st_mtimespec vs st_mtim, st_blocks interpretation) and
  available interfaces (TIOCGWINSZ ioctl). Without pkg/sys each cmd/ package would
  import syscall or golang.org/x/sys directly, duplicating the Darwin/Linux divergence
  handling and making it harder to test or audit the syscall surface.

  pkg/sys provides a stable, tested interface for these three capability areas. Each
  capability is driven by at least one concrete utility requirement derived from C source
  analysis (see dependency-map.yaml).

goals:
  - id: G1
    goal: Provide a portable terminal-width query that works on Darwin and Linux without cgo.
  - id: G2
    goal: Expose extended file metadata (st_nlink, st_uid, st_gid, st_rdev, st_blocks, st_ino, st_dev) not available from os.FileInfo.
  - id: G3
    goal: Provide a SIGPIPE handler that suppresses the broken-pipe error when stdout is closed by a downstream consumer.
  - id: G4
    goal: Provide a SIGWINCH handler that calls a user-supplied callback when the terminal is resized.

requirements:
  - group: R1
    title: Terminal width
    items:
      - id: R1.1
        requirement: Must provide a TerminalWidth() (int, error) function that returns the current terminal column count by calling ioctl(TIOCGWINSZ) on stdout.
      - id: R1.2
        requirement: Must return an error (not panic) when stdout is not a terminal or when ioctl fails.
      - id: R1.3
        requirement: Must use golang.org/x/term or syscall to perform the ioctl; must not shell out to tput or stty.
      - id: R1.4
        requirement: Utility context — ls (ls.c:2281): ls uses struct winsize to determine column count for its default multi-column layout and for the -w/--width flag default.

  - group: R2
    title: Extended file metadata
    items:
      - id: R2.1
        requirement: Must provide a Stat(path string) (*FileInfo, error) function and an Lstat(path string) (*FileInfo, error) function that return a FileInfo struct containing all fields from syscall.Stat_t.
      - id: R2.2
        requirement: FileInfo must include at minimum: Mode (os.FileMode), Size (int64), Nlink (uint64), Uid (uint32), Gid (uint32), Rdev (uint64), Dev (uint64), Ino (uint64), Blocks (int64), Blksize (int64), ModTime (time.Time), and the underlying os.FileInfo for compatibility with os package functions.
      - id: R2.3
        requirement: Must abstract the Darwin/Linux divergence in struct field names (st_mtimespec on Darwin vs st_mtim on Linux; st_blocks interpretation) so callers use a single API.
      - id: R2.4
        requirement: Utility context — ls (ls.c:213, 371): ls needs st_nlink, st_uid, st_gid, st_rdev, st_mode for -l format; st_blocks for -s; st_ino for -i.
      - id: R2.5
        requirement: Utility context — du (du.c:554): du needs st_dev and st_ino for hard-link deduplication, and st_blocks for physical block-count accumulation.
      - id: R2.6
        requirement: Utility context — find (find/ftsfind.c:172): find predicates (-type, -size, -newer, -perm, -uid, -gid) require st_mode, st_size, st_mtime, st_uid, st_gid, st_nlink from the stat struct.

  - group: R3
    title: SIGPIPE handling
    items:
      - id: R3.1
        requirement: Must provide an InstallSIGPIPEHandler() function that suppresses the broken-pipe error when writing to a closed stdout or stderr, causing the process to exit 0 rather than with a write error.
      - id: R3.2
        requirement: Must not use signal.Ignore; instead must install a signal handler that calls os.Exit(0) on SIGPIPE so deferred cleanup functions do not run (matching GNU behavior where SIGPIPE terminates silently).
      - id: R3.3
        requirement: Utility context — ls (ls.c:1600-1620): ls installs a SIGPIPE handler via sigaction so that piping ls output to head does not produce a broken-pipe error message. du, find, and xargs have the same requirement.

  - group: R4
    title: SIGWINCH handling
    items:
      - id: R4.1
        requirement: Must provide an OnTerminalResize(callback func(width int)) function that registers a SIGWINCH handler. When the terminal is resized, the handler must call TerminalWidth() and invoke the callback with the new width.
      - id: R4.2
        requirement: Must support multiple calls to OnTerminalResize; each registered callback must be called on resize.
      - id: R4.3
        requirement: Utility context — ls (ls.c:2281, SIGWINCH registration): ls re-queries terminal width on SIGWINCH to re-format output when the terminal window is resized during a long listing.

non_goals:
  - pkg/sys does not wrap all of syscall or golang.org/x/sys; it exposes only the surfaces required by the current utility batch.
  - pkg/sys does not provide file-watching or inotify/kqueue interfaces.
  - pkg/sys does not handle SIGUSR1/SIGUSR2 (find's debug output); those are deferred to the find PRD.
  - pkg/sys does not provide process-group or terminal control (TIOCGPGRP, tcsetpgrp); those are deferred to the xargs PRD.

acceptance_criteria:
  - id: AC1
    criterion: TerminalWidth() returns a positive integer when called in a test with a pseudo-terminal (golang.org/x/term's test helpers or os.Pipe() as a TTY substitute is acceptable).
  - id: AC2
    criterion: Stat() and Lstat() on a known file return FileInfo with correct Nlink, Uid, Gid, Dev, Ino, and Blocks values as confirmed by running stat(1) on the same file.
  - id: AC3
    criterion: Lstat() on a symlink returns the symlink's own st_mode (type bits show symlink), not the target's mode.
  - id: AC4
    criterion: InstallSIGPIPEHandler() causes a process writing to a broken pipe to exit 0 rather than printing a write error.
  - id: AC5
    criterion: All pkg/sys code compiles on both darwin/arm64 and linux/amd64 with no build errors and no cgo.
  - id: AC6
    criterion: All pkg/sys code compiles with no warnings under golangci-lint.
